{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://local.spec/animagine/prompt.schema.json",
  "title": "Prompt (animagine/spec/**/*.json)",
  "description": "One prompt per JSON file under animagine/spec/, including subfolders. The path relative to animagine/spec/ without .json is the spec id. ComfyUI filename_prefix is that id (nested folders preserved). Specified-prompt image generation; not cartesian LoRA training rows.",
  "type": "object",
  "additionalProperties": false,
  "required": ["image_size", "positive", "negative"],
  "properties": {
    "lora": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id"],
      "description": "Optional LoRA applied via ComfyUI LoraLoader. One per prompt. Omit lora entirely when unused.",
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Filename on the ComfyUI server under models/loras, including optional subfolders (e.g. style/foo.safetensors)."
        },
        "strength_model": {
          "type": "number",
          "description": "ComfyUI LoraLoader strength_model. When omitted, 1 is used."
        },
        "strength_clip": {
          "type": "number",
          "description": "ComfyUI LoraLoader strength_clip. When omitted, 1 is used."
        }
      }
    },
    "pose": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id"],
      "description": "Optional pose. Omit pose entirely when unused.",
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Filename on the ComfyUI server under input."
        }
      }
    },
    "image_size": {
      "type": "object",
      "additionalProperties": false,
      "required": ["width", "height"],
      "properties": {
        "width": { "$ref": "#/$defs/image_dimension" },
        "height": { "$ref": "#/$defs/image_dimension" }
      },
      "description": "Output dimensions (width x height). Animagine XL 4: each side must be a multiple of 64 and at most 1536."
    },
    "positive": {
      "$ref": "#/$defs/tags",
      "description": "Positive prompt tags, joined for ComfyUI. Trigger words belong here. Empty emits no positive tags."
    },
    "negative": {
      "$ref": "#/$defs/tags",
      "description": "Negative prompt tags, joined for ComfyUI. Empty emits no negative tags."
    }
  },
  "$defs": {
    "image_dimension": {
      "type": "integer",
      "minimum": 64,
      "maximum": 1536,
      "multipleOf": 64,
      "description": "Animagine XL 4 pixel length. Multiple of 64, at most 1536 (recommended range)."
    },
    "tags": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    }
  }
}
